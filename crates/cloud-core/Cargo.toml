[package]
name = "cloud-core"
version.workspace = true
description = "Shared primitives for hand-written Chinese cloud SDKs: crypto/signing helpers, errors, HTTP"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
thiserror = { workspace = true }
reqwest = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
async-stream = "0.3"
hmac = "0.12"
sha1 = "0.10"
sha2 = "0.10"
md-5 = "0.10"
hex = "0.4"
base64 = "0.22"

[dev-dependencies]
# test-util 提供 tokio::test(start_paused) 的虚拟时钟,用于确定性地测限速。
tokio = { workspace = true, features = ["test-util"] }
